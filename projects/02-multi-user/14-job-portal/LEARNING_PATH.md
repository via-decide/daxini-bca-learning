## 🎯 Learning Outcomes
✅ **Complex Filtering** - Querying ranges (`min_salary <= X AND max_salary >= Y`).
✅ **File Uploads** - Handling `multipart/form-data` and storing file paths.
✅ **Pagination** - Returning 10 jobs at a time instead of 10,000.

## 📋 Project Overview
Build a job board where companies post roles and applicants submit their resumes. Includes a robust search engine for job seekers.
