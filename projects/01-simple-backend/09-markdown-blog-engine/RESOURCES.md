# 📝 Markdown Blog Engine: Learn By Building

**"Build a fast, file-based blog engine that reads Markdown files from the hard drive, parses them into HTML, and serves them to users without needing a database."**

---

## 📚 Resources

**Core Node.js Libraries:**
- Node `fs` (File System) Module: https://nodejs.org/api/fs.html
- `fs.readdirSync` vs `fs.promises.readdir` (Understanding blocking vs non-blocking I/O): https://nodejs.org/en/learn/manipulating-files/reading-files-with-nodejs

**Markdown & Parsing Libraries:**
- `gray-matter` (The industry standard for parsing YAML frontmatter): https://github.com/jonschlinkert/gray-matter
- `marked` (A very fast markdown parser): https://marked.js.org/

**Security Concepts:**
- Path Traversal Vulnerabilities (OWASP): https://owasp.org/www-community/attacks/Path_Traversal

---
